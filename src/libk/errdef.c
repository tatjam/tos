#include "errdef.h"

void kerror(char msg[128])
{
	// TODO: Serial logging
	/*ktty_set_active_color(vga_set_bright(VGA_RED, true), VGA_BLACK);
	ktty_puts("[ERROR] ");
	msg[127] = '\0';
	ktty_puts(msg);*/
}